# KonomiTV

tsukumijima/KonomiTV v0.14.1（commit `0a32188`）です。サードパーティは同じリリースの `thirdparty-linux.tar.xz` です。

番組表・予約・放送波は EDCB です。録画先のディレクトリ名は `config.yaml` の `video.recorded_folders` です。`/media` は読み書きでマウントします。
