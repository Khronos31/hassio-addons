# EDCB

xtne6f/EDCB `work-plus-s` の `ebf50c7` と、matching/BonDriver_LinuxMirakc の `cfbefc6` を Debian 上でビルドするアドオンです。PX-S1UD は同梱の BonDriver_S1UD が開きます。

起動直後のチューナー本数は 0 です。本数は `EpgTimerSrv.ini` の `Count` です。詳細は DOCS.md です。
